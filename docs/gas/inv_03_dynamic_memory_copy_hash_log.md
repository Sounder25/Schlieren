## Dynamic Opcode, Memory, Copy, Hash, and Log Gas

| Proposed Rule ID | Operation | Current Formula/Constant | Inputs and Branches | Fork Behavior | Charge/Movement Kind | Production Source | Existing Tests | Findings |
|---|---|---|---|---|---|---|---|---|
| MEMORY.EXPANSION | Memory expansion cost | `Δcost = (3·new_words + ⌊new_words²/512⌋) - (3·old_words + ⌊old_words²/512⌋)` where `words = ceil(size/32)` | `newSize` (bytes), current `_data.Length` | All forks: identical formula (Yellow Paper §9.2) | Charge | `Scrutor.Core/Execution/EvmMemory.cs:51-63` | None found (tested implicitly through MLOAD/MSTORE conformance) | Formula correct. Max size capped at 16 MB (line 8). No overflow protection on `newCost - oldCost` if `newWords` is very large — subtraction assumes `newCost ≥ oldCost`, which holds when `newSize > oldSize`. |
| OP.MLOAD | MLOAD(0x51) | `3 + MEMORY.EXPANSION(offset + 32)` | `offset` (stack pop), memory state | All forks: 3 base + expansion | Charge | `Scrutor.Core/Opcodes/MemoryOpcodes.cs:21-22` | None found | Uses `ConsumeGas` internally and returns `Success(0)` — different pattern from opcodes that return their cost. Schedule implementer must record 3 + expansion as a single Charge entry in the journal. |
| OP.MSTORE | MSTORE(0x52) | `3 + MEMORY.EXPANSION(offset + 32)` | `offset` (stack pop), memory state | All forks: 3 base + expansion | Charge | `Scrutor.Core/Opcodes/MemoryOpcodes.cs:48-49` | None found | Same `ConsumeGas`+`Success(0)` pattern as MLOAD |
| OP.MSTORE8 | MSTORE8(0x53) | `3 + MEMORY.EXPANSION(offset + 1)` | `offset` (stack pop), memory state | All forks: 3 base + expansion | Charge | `Scrutor.Core/Opcodes/MemoryOpcodes.cs:82-83` | None found | Same `ConsumeGas`+`Success(0)` pattern |
| OP.MCOPY | MCOPY(0x5E) | `3 + 3·ceil(length/32) + MEMORY.EXPANSION(max(dst+length, src+length))`; if `length == 0`: only base 3, no expansion | `dst`, `src`, `length` (stack pops), memory state | Cancun+: active (EIP-5656); pre-Cancun: INVALID opcode | Charge | `Scrutor.Core/Opcodes/MemoryCopyOpcode.cs:38-56` | None found | Zero-length short-circuit at line 35-40 returns `Success(3)` skipping expansion correctly |
| OP.COPY_WORD | CALLDATACOPY(0x37) CODECOPY(0x39) RETURNDATACOPY(0x3E) | `3 + 3·ceil(length/32) + MEMORY.EXPANSION(destOffset + length)` | `destOffset`, `offset`, `length` (stack pops), memory state | All forks: identical formula | Charge | `Scrutor.Core/Opcodes/ExecutionOpcodes.cs:279-291,332-345,391-403` | None found | No issue observed |
| OP.EXTCODECOPY | EXTCODECOPY(0x3C) | `rules.ExtAccountCost(isWarm) + 3·ceil(length/32) + MEMORY.EXPANSION(destOffset + length)` | `addr`, `destOffset`, `offset`, `length` (stack pops); `isWarm = context.Access.TouchAddress(address)`, `rules.HasEip2929WarmCold` | Frontier=20+copy+mem; TangerineWhistle=700+copy+mem; Berlin+=warm(100)/cold(2600)+copy+mem | Charge | `Scrutor.Core/Opcodes/StateOpcodes.cs:131-158` | None found | Access cost delegated to `rules.ExtAccountCost(isWarm)` — fork-aware. Copy and memory expansion hardcoded. No issue observed. |
| OP.KECCAK256 | KECCAK256(0x20) | `30 + 6·ceil(length/32) + MEMORY.EXPANSION(offset + length)` | `offset`, `length` (stack pops), memory state | All forks: identical formula | Charge | `Scrutor.Core/Opcodes/StateOpcodes.cs:27-39` | None found | Constants 30 and 6 are hardcoded, not in ForkRules. No issue observed |
| OP.LOG | LOG0–LOG4 (0xA0–0xA4) | `375 + 375·topic_count + 8·data_length + MEMORY.EXPANSION(offset + length)` | `offset`, `length` (stack pops); `TopicCount` (class constant 0–4), memory state | All forks: identical formula | Charge | `Scrutor.Core/Opcodes/LoggingOpcodes.cs:35,47` | None found | `375`, `375·k`, `8·byte` are hardcoded in the shared `LogOpcodeBase`. Memory expansion uses `offsetInt + lengthInt` without overflow guard — could be an issue for very large inputs, but `OperandValidation` would catch it upstream. |

{
  "commands": [
    "set -eu",
    "export HOME=/root",
    "export DOTNET_CLI_HOME=/root",
    "export DOTNET_CLI_TELEMETRY_OPTOUT=1",
    "export PATH=/usr/share/dotnet:/usr/local/bin:$PATH",
    "cd /opt/schlieren",
    "git rev-parse HEAD",
    "dotnet publish Schlieren.CLI/Schlieren.CLI.csproj -c Release -o /opt/schlieren-guard",
    "ls -la /opt/schlieren-guard",
    "/opt/schlieren-guard/Schlieren.CLI --help"
  ]
}

{
  "commands": [
    "export DEBIAN_FRONTEND=noninteractive",
    "apt-get update -qq",
    "apt-get install -y -qq git curl ca-certificates",
    "git ls-remote https://github.com/Sounder25/Schlieren.git refs/heads/feature/schlieren-guard; echo CLONE_PROBE=$?"
  ]
}
